# Страны мира

Небольшой клиентский квиз на React + TypeScript + Vite. В одном раунде 10 случайных вопросов, ответы не повторяются внутри раунда, а варианты каждый раз перемешиваются.

## Установка

```bash
npm install
```

## Запуск

```bash
npm run dev
```

После запуска Vite покажет локальный адрес приложения, обычно `http://localhost:5173`.

## Режимы игры

- «Угадай, чья это столица»: игрок видит столицу и выбирает правильную страну.
- «Угадай, чей это флаг»: игрок видит название страны и выбирает правильный флаг.

Данные стран находятся в `src/data/countries.ts`, логика генерации вопросов и расчёта результата — в `src/utils/quiz.ts`.
